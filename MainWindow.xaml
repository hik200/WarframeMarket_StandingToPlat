<Window x:Class="WarframeMarket_StandingToPlat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Warframe Market Standing to Plat" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a1a">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#2d2d2d" Padding="20">
            <StackPanel>
                <TextBlock Text="Warframe Market Standing to Plat" 
                          FontSize="24" FontWeight="Bold" 
                          Foreground="White" HorizontalAlignment="Center"/>
                <TextBlock Text="Convert your syndicate standing into platinum!" 
                          FontSize="14" 
                          Foreground="#cccccc" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left Panel - Controls -->
            <Border Grid.Column="0" Background="#2d2d2d" CornerRadius="12" Padding="25" Margin="0,0,15,0">
                <StackPanel>
                    <!-- Header -->
                    <TextBlock Text="Select Syndicate" FontSize="18" FontWeight="Bold" 
                              Foreground="#ffffff" Margin="0,0,0,20"/>
                    
                    <!-- Syndicate Dropdown -->
                    <TextBlock Text="Choose your syndicate:" FontSize="12" 
                              Foreground="#b0b0b0" Margin="0,0,0,8"/>
                    <ComboBox x:Name="SyndicateComboBox" Height="40" FontSize="14"
                              Background="#ffffff" Foreground="#2d2d2d" BorderBrush="#e0e0e0"
                              BorderThickness="2" Padding="10,8"
                              SelectionChanged="SyndicateComboBox_SelectionChanged"/>
                    
                    <!-- Action Buttons -->
                    <Button x:Name="FetchButton" Content="🔍 Fetch Market Data" 
                            Height="45" FontSize="14" FontWeight="Bold"
                            Background="#4CAF50" Foreground="White" BorderThickness="0"
                            Margin="0,25,0,0" Click="FetchButton_Click"
                            IsEnabled="False">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="8" Padding="15,10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#45a049"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#3d8b40"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Background" Value="#666666"/>
                                                    <Setter Property="Foreground" Value="#999999"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <Button x:Name="ExportButton" Content="📁 Export Results" 
                            Height="40" FontSize="13" FontWeight="SemiBold"
                            Background="#2196F3" Foreground="White" BorderThickness="0"
                            Margin="0,12,0,0" Click="ExportButton_Click"
                            IsEnabled="False">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="6" Padding="12,8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#1976D2"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#1565C0"/>
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter Property="Background" Value="#666666"/>
                                                    <Setter Property="Foreground" Value="#999999"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <Button x:Name="LoadOldDataButton" Content="📂 Load Old Data" 
                            Height="40" FontSize="13" FontWeight="SemiBold"
                            Background="#FF9800" Foreground="White" BorderThickness="0"
                            Margin="0,12,0,0" Click="LoadOldDataButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="6" Padding="12,8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#F57C00"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#E65100"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    
                    <!-- Status Section -->
                    <Border Background="#1a1a1a" CornerRadius="8" Padding="15" Margin="0,25,0,0">
                        <StackPanel>
                            <TextBlock Text="📊 Status" FontSize="13" FontWeight="Bold" 
                                      Foreground="#ffffff" Margin="0,0,0,8"/>
                            <TextBox x:Name="StatusText" Text="Ready" FontSize="12"
                                     Foreground="#b0b0b0" Background="#1a1a1a" BorderThickness="0"
                                     IsReadOnly="True" IsReadOnlyCaretVisible="True"
                                     TextWrapping="Wrap" AcceptsReturn="True"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>
            
            <!-- Right Panel - Results -->
            <Border Grid.Column="1" Background="#2d2d2d" CornerRadius="8" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Market Results" FontSize="16" FontWeight="Bold" 
                              Foreground="White" Margin="0,0,0,15"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="ResultsPanel">
                            <TextBlock Text="Select a syndicate and click 'Fetch Market Data' to begin." 
                                      FontSize="14" Foreground="#cccccc" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      TextAlignment="Center"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Footer -->
        <Border Grid.Row="2" Background="#1a1a1a" Padding="10">
            <TextBlock Text="Warframe Market API Integration" FontSize="10" 
                      Foreground="#888888" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
